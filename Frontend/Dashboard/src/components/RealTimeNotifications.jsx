import React from 'react';

/**
 * Placeholder RealTimeNotifications component
 * This component was referenced but didn't exist, causing import errors
 * It's now a simple placeholder that doesn't break the dashboard
 */
const RealTimeNotifications = ({ adminData }) => {
  // This component is intentionally empty to prevent import errors
  // The real-time notification functionality is handled by other services
  return null;
};

export default RealTimeNotifications;
